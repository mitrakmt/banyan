import React from 'react'
import 'rotatingView.css'

const rotatingView = ({ children }) => (
  <div className="banyan-rotatingView-container">
    {React.children.map(children, (child) => {
      
    })}
  </div>
)

export default rotatingView